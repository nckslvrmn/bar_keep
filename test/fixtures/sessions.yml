active_session:
  user: regular
  session_token: active_token_123
  expires_at: <%= 30.days.from_now %>

expired_session:
  user: regular
  session_token: expired_token_456
  expires_at: <%= 1.day.ago %>
