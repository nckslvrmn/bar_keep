<turbo-stream action="morph" target="item_row_<%= @item.id %>">
  <template>
    <%= render partial: "item_row", formats: [:html], locals: { item: @item, show_owner: current_user&.guest?, show_actions: !current_user&.guest? } %>
  </template>
</turbo-stream>
